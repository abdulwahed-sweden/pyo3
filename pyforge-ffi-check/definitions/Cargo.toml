[package]
name = "pyforge-ffi-check-definitions"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
pyforge-ffi = { path = "../../pyforge-ffi" }

[build-dependencies]
bindgen = "0.72"
pyforge-build-config = { path = "../../pyforge-build-config" }
