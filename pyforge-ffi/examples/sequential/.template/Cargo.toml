[package]
authors = ["{{authors}}"]
name = "{{project-name}}"
version = "0.1.0"
edition = "2021"

[lib]
name = "sequential"
crate-type = ["cdylib", "lib"]

[dependencies]
pyforge-ffi = { version = "{{PYFORGE_VERSION}}" }
