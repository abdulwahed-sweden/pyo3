[package]
name = "sequential"
version = "0.1.0"
edition = "2021"
rust-version = "1.83"

[lib]
name = "sequential"
crate-type = ["cdylib", "lib"]

[dependencies]
pyforge-ffi = { path = "../../" }

[build-dependencies]
pyforge-build-config = { path = "../../../pyforge-build-config" }

[workspace]
